# Модуль 2. Машинное обучение в поиске

**Профессиональная роль:** ML Researcher / ML Engineer


## Назначение ФОС

Фонд оценочных средств предназначен для оценки уровня сформированности профессиональных компетенций обучающихся в области практической реализации алгоритмов ранжирования на основе машинного обучения, а также понимания архитектурных подходов к построению поисковых систем.

В рамках модуля оценивается способность обучающегося:

- различать подходы *pointwise*, *pairwise* и *listwise* в задаче ранжирования;
- понимать архитектурные особенности различных моделей сопоставления запроса и документа (Bi-encoder, Cross-encoder, CDSSM);
- реализовывать полный пайплайн Learning to Rank (LTR) с использованием градиентного бустинга;
- управлять процессом ранжирования через инженерию признаков и гиперпараметры;
- оптимизировать метрику качества списка NDCG;
- интерпретировать результаты моделирования и анализировать вклад признаков;
- обосновывать выбор архитектуры и методов в зависимости от бизнес-контекста;
- выявлять и корректировать проблемы, связанные с дисбалансом данных, позиционным смещением и холодным стартом.


## Связь с компетенциями

| Индикатор | Занятие | Формы контроля |
| :--- | :--- | :--- |
| ML-2.1 | 2.1 | Контрольные вопросы, практические задания, тестирование |
| ML-2.2 | 2.2 | Контрольные вопросы, практические задания, тестирование |
| ML-3.2 | 2.3 | Лабораторная работа |


## Перечень оценочных средств

| № | Название | Тип | Индикатор |
| :--- | :--- | :--- | :--- |
| 1 | Задание «Архитектурный пинг-понг» (Pointwise, Pairwise, Listwise) | Текущий | ML-2.1 |
| 2 | Задание «Слепая дегустация эмбеддингов» | Текущий | ML-2.2 |
| 3 | Эссе-миниатюра по инженерии признаков | Текущий | ML-2.1 |
| 4 | Лабораторная работа «Реализация алгоритма ранжирования на основе машинного обучения» | Текущий | ML-3.2 |
| 5 | Итоговый тест | Итоговый | ML-2.1, ML-2.2, ML-3.2 |


## Критерии оценивания

### Шкала перевода баллов

| Оценка | Баллы (max 100) |
| :--- | :--- |
| Отлично | 86–100 |
| Хорошо | 70–85 |
| Удовлетворительно | 51–69 |
| Неудовлетворительно | 0–50 |


### Распределение баллов по модулю

| Элемент контроля | Баллы |
| :--- | :--- |
| Задание «Архитектурный пинг-понг» (аудиторная работа) | 10 |
| Задание «Слепая дегустация эмбеддингов» (аудиторная работа) | 10 |
| Эссе-миниатюра по инженерии признаков | 10 |
| Лабораторная работа (LTR-пайплайн) | 45 |
| Итоговый тест | 25 |
| **Итого** | **100** |


## Методические рекомендации

1. **Задание «Архитектурный пинг-понг»** проводится в парах на семинарском занятии. Оценивается полнота заполнения таблицы и качество найденных логических ошибок в работе других пар.
2. **Задание «Слепая дегустация эмбеддингов»** проводится фронтально с использованием карточек «А», «Б», «В». Оценивается скорость и правильность идентификации архитектур.
3. **Эссе-миниатюра** выполняется индивидуально в письменной форме. Оценивается глубина понимания проблемы и обоснованность предложенного решения.
4. **Лабораторная работа** выполняется индивидуально с использованием Jupyter Notebook. Оценивается как реализация (код), так и аналитическая часть (визуализации и выводы).
5. **Итоговый тест** проводится в письменной или электронной форме и включает 20 вопросов закрытого типа по всем темам модуля.


## Материально-техническое обеспечение

- Jupyter Notebook / Python 3.10+
- Библиотеки: pandas, numpy, scikit-learn, lightgbm, xgboost, matplotlib, seaborn
- Доступ к данным: логи поиска (query, document, clicks, position)
- Проектор для демонстрации (задания 2 и 3)
- Карточки для голосования (задание 2)


## Содержание оценочных средств

### Блок 1. Текущий контроль (по лекционному курсу — 4 часа)

#### 1.1 Задание «Архитектурный пинг-понг» (Pointwise, Pairwise, Listwise)

**Время выполнения:** 15 минут.

Студенты разбиваются по парам. На листе нарисована таблица:

| | Pointwise | Pairwise | Listwise |
| :--- | :--- | :--- | :--- |
| **Сбор данных** | | | |
| **Выбор функции потерь** | | | |
| **Оптимизация метрики** | | | |
| **A/B-тест** | | | |

**Ход выполнения:**

- **Раунд 1 (3 мин):** Первый студент заполняет одну любую ячейку для Pointwise (например: «Функция потерь: MSE»). Передает лист партнеру.
- **Раунд 2 (3 мин):** Второй студент заполняет аналогичную ячейку для Pairwise, указывая принципиальное отличие (например: «Функция потерь: попарное сравнение вероятностей RankNet»).
- **Раунд 3 (3 мин):** Первый студент переходит к Listwise, заполняя соответствующую ячейку.
- **Финал (6 мин):** Пары обмениваются листами с другой парой. Задача — найти в чужой таблице логическую ошибку или дублирование.

**Критерии оценивания:**

| Критерий | Баллы |
| :--- | :--- |
| Заполнение всех ячеек в рамках своей пары | 5 |
| Корректное определение отличий между подходами | 3 |
| Найденные ошибки в чужой таблице (каждая) | 1 (max 2) |
| **Итого** | **10** |


#### 1.2 Задание «Слепая дегустация эмбеддингов»

**Время выполнения:** 15 минут.

Преподаватель выводит на экран 3 пары [Запрос — Документ]. Для каждой пары показаны два варианта оценки релевантности от разных моделей.

**Вариант А (Bi-encoder/Siamese):** Косинусное сходство усредненных векторов. Оценка высокая, но текст содержит слова-синонимы, которых нет в запросе.

**Вариант Б (Cross-encoder):** Проход [Query+Doc] через BERT. Оценка ниже, так как модель увидела отсутствие прямого взаимодействия слов, но уловила отрицание.

**Вариант В (Ложный CDSSM):** Свертка по буквам. Оценка очень высокая при наличии опечатки в запросе.

**Ход выполнения:**

1. Студенты видят пару и три оценки. У каждого есть карточки «А», «Б», «В».
2. За 90 секунд они должны решить, какая архитектура стоит за каждым вариантом, исходя из того, как модель обрабатывает контекст, порядок слов и шум.
3. На счет «три» все поднимают карточку одновременно.

**Критерии оценивания:**

| Критерий | Баллы |
| :--- | :--- |
| Правильная идентификация архитектуры (за каждую пару) | 3 (max 9) |
| Обоснование выбора (устное пояснение) | 1 |
| **Итого** | **10** |


#### 1.3 Эссе-миниатюра по инженерии признаков

**Время выполнения:** 10 минут.

**Задание:**

Вы создаете поиск для сервиса доставки цветов. У вас есть идеальный поведенческий признак: Conversion_Rate (процент заказов после просмотра).

Опишите ситуацию (3 предложения), когда использование этого чистого сигнала обрушит выручку компании. Предложите инженерную модификацию признака (2 предложения) или способ его коррекции в функции потерь (1 предложение).

**Критерии оценивания:**

| Критерий | Баллы |
| :--- | :--- |
| Глубина понимания проблемы (ситуация разрушения бизнеса) | 4 |
| Обоснованность предложенной модификации признака | 4 |
| Четкость и лаконичность изложения | 2 |
| **Итого** | **10** |


### Блок 2. Лабораторный практикум (8 часов)

#### 2.1 Лабораторная работа

**Тема:** Реализация алгоритма ранжирования на основе машинного обучения.

**Цель работы:** Реализовать полный пайплайн Learning to Rank (LTR): от предобработки сырых данных до оптимизации метрики качества списка (NDCG) с помощью градиентного бустинга. Научиться управлять процессом ранжирования через инженерию признаков и гиперпараметры.

**Инструментарий:** Python, Jupyter Notebook, библиотеки pandas, numpy, scikit-learn, lightgbm, xgboost.

**Задание:**

1. **Подготовка данных:**
   - Загрузить логи поиска (query, document, clicks, position).
   - Сгруппировать документы по query_id.
   - Сгенерировать матрицу признаков:
     - Текстовый признак: BM25 сходство запроса и заголовка.
     - Поведенческий признак: CTR × 1/log(1 + position).
     - Статический признак: длина документа.
   - Задать целевую переменную релевантности (y) на основе кликов (например: клик = 2, показ без клика = 1).

2. **Baseline Pointwise:**
   - Обучить модель HistGradientBoostingRegressor в pointwise-режиме для предсказания оценки y.
   - Отсортировать документы внутри каждой группы-запроса по убыванию предсказанного Score.
   - Зафиксировать метрику NDCG@5 как baseline.

3. **Listwise-оптимизация LambdaMART:**
   - Переобучить модель с использованием LGBMRanker (или XGBRanker) с параметром objective='lambdarank'.
   - Указать метрику качества в параметрах: metric='ndcg', eval_at=[1, 5, 10].
   - Подобрать гиперпараметры (глубина деревьев, количество листьев) для максимизации NDCG@5.
   - Сравнить результат со значением baseline из п. 2.

4. **Подготовка отчета (Jupyter Notebook):**
   - Визуализация важности признаков (Feature Importance).
   - График роста метрики NDCG в процессе обучения.
   - Краткий вывод: какой тип признаков (текстовые или поведенческие) внес наибольший вклад в качество топ-5 выдачи и почему.

**Критерии оценивания лабораторной работы:**

| Оценка | Уровень выполнения | Баллы |
| :--- | :--- | :--- |
| **Отлично** | Продвинутый уровень. Пайплайн работает без ошибок, реализован качественный feature engineering, показан значительный рост метрики NDCG@5 (более 10% к baseline), даны глубокие аналитические выводы по важности признаков и проблеме холодного старта. | 41–45 |
| **Хорошо** | Уверенный технический уровень. Код рабочий, Baseline и Listwise-модели обучены корректно. Рост метрики присутствует, но может быть незначительным или нестабилен. В визуализациях есть мелкие недочеты или неполные пояснения. | 31–40 |
| **Удовлетворительно** | Базовый уровень. Выполнены основные требования: данные загружены, Pointwise-модель обучена. Переход к Listwise-оптимизации выполнен формально (часто с дефолтными параметрами) или содержит ошибки в логике группировки данных. | 21–30 |
| **Неудовлетворительно** | Критический уровень. Отсутствует логика группировки документов по query_id, код выдает RuntimeError на этапе обучения, концепция оптимизации списка вместо одиночного объекта не усвоена. | 0–20 |


**Вопросы к защите лабораторной работы:**

1. Объясните, зачем нужен инвертированный индекс и как он работает в поиске.
2. Перечислите и поясните факторы, влияющие на ранжирование документов в поисковых системах.
3. Какой вклад вносят технологии машинного обучения в интеллектуальные поисковые системы?
4. Дайте определение и приведите примеры типов запросов (коммерческий, навигационный, информационный).


### Блок 3. Итоговый контроль

#### 3.1 Итоговый тест

Итоговый тест включает 20 вопросов закрытого типа. Время выполнения — 40 минут.

**Вопросы:**

**1. Какая метрика качества является основной целью оптимизации в классическом *listwise*-подходе (например, в LambdaMART), в отличие от *pointwise*?**

а) Точность классификации отдельного документа.

б) Среднеквадратичная ошибка предсказания оценки релевантности.

в) Нормализованная дисконтированная накопительная выгода (NDCG) или ее аппроксимация.

г) Евклидово расстояние между векторами запроса и документа.

---

**2. В чем фундаментальное различие между целевой функцией *RankNet* (*pairwise*) и *LambdaLoss* (*listwise*)?**

а) RankNet работает только с текстовыми признаками, а LambdaLoss — с поведенческими.

б) RankNet минимизирует количество неправильно упорядоченных пар документов, тогда как LambdaLoss напрямую штрафует падение значения NDCG при перестановке документов в списке.

в) RankNet использует градиентный бустинг, а LambdaLoss — только нейронные сети.

г) Различий нет, это разные названия одной и той же функции потерь.

---

**3. Вы строите модель ранжирования для новой поисковой системы, где полностью отсутствуют логи кликов, но есть небольшая команда асессоров. Какой подход наиболее эффективен на старте?**

а) Listwise, так как он лучше всего масштабируется на миллиарды запросов.

б) Pointwise, так как задачу можно свести к регрессии: обучить модель предсказывать явную оценку релевантности (0–4 балла), выставленную асессором каждому документу независимо.

в) Pairwise, так как асессорам проще сравнивать документы парами, чем ставить баллы.

г) Использование только BM25 без машинного обучения.

---

**4. При каком сценарии использование *pairwise*-подхода (например, модели AdaRank) будет вычислительно более предпочтительным, чем *listwise*?**

а) Когда длина поисковой выдачи всегда строго равна 10 документам.

б) Когда объем обучающих данных огромен (миллиарды пар клик-не-клик), а офлайн-метрики не требуют точной оптимизации хвоста распределения; важна скорость конвертации сырых логов в тренировочный сет.

в) Когда необходимо учитывать взаимное влияние документа на позиции 1 и 10 одновременно.

г) Когда все признаки являются категориальными.

---

**5. Что из перечисленного НЕ является входными данными (features) для *pointwise*-модели?**

а) TF-IDF слов запроса в документе.

б) PageRank URL-адреса документа.

в) Время суток, когда был совершен запрос.

г) Относительный порядок документа docA по отношению к документу docB в текущей выдаче.

---

**6. Основная проблема прямого применения *pointwise*-регрессии для задачи ранжирования заключается в том, что...**

а) Она требует слишком много оперативной памяти.

б) Минимизация ошибки предсказания рейтинга (MSE) не гарантирует оптимального порядка документов: маленькая ошибка в оценке топ-документа критичнее, чем большая ошибка внизу списка.

в) Она не поддерживает линейные признаки.

г) Она не может работать с эмбеддингами.

---

**7. В контексте *pairwise*-обучения, что такое «тяжелый негатив» (hard negative)?**

а) Документ, который вообще не содержит слов из запроса.

б) Документ, который нерелевантен запросу, но настолько похож на него лексически или семантически, что модель склонна ошибиться и поставить его выше истинно релевантного.

в) Документ с самым низким значением PageRank в выборке.

г) Документ, вызвавший быстрый уход пользователя с сайта (pogo-sticking).

---

**8. Если бизнес-цель поиска — максимизировать вероятность того, что пользователь найдет хотя бы один подходящий товар и совершит покупку (конверсию), какой уровень детализации loss-функции наиболее адекватен?**

а) Pointwise, так как покупка привязана к одному товару.

б) Pairwise, чтобы гарантировать превосходство купленного товара над остальными.

в) Listwise, так как конверсия зависит от наличия качественного выбора во всей видимой части выдачи (diversity и наличие резерва на случай отказа).

---

**9. К какому типу относится признак «Косинусное сходство между эмбеддингом запроса и эмбеддингом заголовка документа, полученное через предобученную модель SBERT»?**

а) Поведенческий признак.

б) Статический документный признак.

в) Текстовый запросно-документный признак глубокого уровня (семантический).

г) Признак свежести контента.

---

**10. Почему прямое использование нормализованного времени пребывания на странице (dwell time) в качестве лейбла или признака может быть опасным (проблема Position Bias)?**

а) Потому что время измеряется в секундах, а модель ожидает числа от 0 до 1.

б) Потому что пользователи проводят больше времени на документах в топе просто потому, что те попали в топ, даже если контент посредственный (эффект самоисполняющегося пророчества).

в) Потому что мобильные устройства загружают страницы медленнее.

г) Никакой опасности нет, это самый чистый сигнал.

---

**11. Выберите пример сложного составного признака (cross-feature):**

а) Количество слов в запросе.

б) Наличие слова «цена» в title документа.

в) Произведение вероятности CTR (из исторической статистики) на коэффициент коммерческой маржинальности категории товара.

г) Язык документа.

---

**12. Для чего в задаче ранжирования часто применяют трансформацию числовых признаков (например, логарифмирование цены или количества просмотров)?**

а) Чтобы скрыть реальные данные от конкурентов.

б) Чтобы уменьшить влияние выбросов и привести распределение признака к нормальному виду, предотвращая доминирование одного сверхпопулярного документа над всеми остальными при обучении линейных моделей.

в) Это требование формата протокола передачи данных gRPC.

г) Логарифмирование увеличивает размерность пространства.

---

**13. Какой тип признака чаще всего страдает от проблемы «холодного старта» (cold start) для новых документов?**

а) Текстовые признаки (BM25).

б) Семантические эмбеддинги.

в) Поведенческие признаки (CTR, Add-to-Favorites, исторические покупки).

г) Геолокационные признаки.

---

**14. В чем преимущество использования гистограммных статистик (например, Bucketized features) вместо непрерывных значений для возраста документа?**

а) Гистограммы занимают меньше места на диске.

б) Они позволяют модели выучить нелинейные зависимости (например, что документы возрастом 1 день и 7 дней сильно различаются по ценности, а 365 и 372 дня — почти нет).

в) Они автоматически решают проблему мультиколлинеарности.

г) Нейросети могут принимать на вход только целые числа.

---

**15. Главное архитектурное отличие классического Gradient Boosting на Decision Trees (GBDT) от End-to-End Deep Ranking моделей заключается в...**

а) Использовании GPU для вычислений.

б) Типе используемой функции активации.

в) Том, что GBDT требует заранее подготовленных человеком фичей, тогда как глубокие сети способны самостоятельно извлекать иерархические представления прямо из сырого текста или изображений.

г) Том, что деревья работают быстрее инференса нейросетей.

---

**16. В архитектуре DSSM (Deep Structured Semantic Model) сверточная сеть (CNN) применяется поверх буквенных триграмм. Какую основную проблему токенизации на уровне слов это решает?**

а) Уменьшает размер словаря.

б) Устойчивость к опечаткам и морфологическим вариациям: слово «автомобиль» и «афтомобль» будут иметь схожее представление на уровне общих буквенных триграмм после свертки.

в) Позволяет обрабатывать языки без пробелов (китайский).

г) Ускоряет обучение в 10 раз.

---

**17. Чем принципиально отличается архитектура Cross-encoder от Bi-encoder (как в DSSM/Siamese networks) при сопоставлении запроса и документа?**

а) Cross-encoder использует только CNN, а Bi-encoder — только Transformer.

б) Cross-encoder пропускает конкатенацию [Query + Document] через единую глубокую сеть, позволяя моделировать сложные взаимодействия между словами запроса и документа, тогда как Bi-encoder кодирует их независимо.

в) Bi-encoder требует ручной разметки пар, а Cross-encoder учится на одиночных кликах.

г) Cross-encoder выдает бинарный ответ (да/нет), а Bi-encoder — вещественное число.

---

**18. Роль Multi-head Self-attention в современных ранжировщиках (например, BERT-based ranking) состоит в том, чтобы...**

а) Увеличить длину контекста до бесконечности.

б) Взвесить важность каждого слова в запросе относительно каждого слова в документе, выявляя специфические паттерны соответствия (например, связь глагола действия в запросе с результатом в тексте).

в) Заменить собой этап извлечения признаков (retrieval).

г) Нормализовать длины векторов перед подачей в полносвязный слой.

---

**19. Почему двухэтапная архитектура (Retriever → Re-ranker) является стандартом индустрии сегодня?**

а) Потому что серверы стали слабее.

б) Из-за требований законодательства о защите персональных данных.

в) Из-за квадратичной сложности внимания (O(n²)) трансформеров: кросс-кодировщик слишком дорог для прохода по миллионам кандидатов, поэтому сначала используется дешевый би-энкодер для сужения круга до топ-50, и только потом подключается тяжелый ранжировщик.

г) Чтобы разделить обязанности между двумя разными дата-саентистами.

---

**20. Архитектура CDSSM (Convolutional DSSM) вводит локально чувствительные слои. Какое смысловое ограничение классических Word Averaging (усреднение word2vec) она помогает преодолеть?**

а) Проблема редких слов (UNK).

б) Игнорирование порядка слов: благодаря окнам свертки модель учитывает, что «купить билет Москва-Питер» отличается от «купить билет Питер-Москва».

в) Высокое энергопотребление дата-центров.

г) Невозможность работы с английским языком.


### Ключи к итоговому тесту

| Вопрос | Ответ | Вопрос | Ответ |
| :---: | :---: | :---: | :---: |
| 1 | в | 11 | в |
| 2 | б | 12 | б |
| 3 | б | 13 | в |
| 4 | б | 14 | б |
| 5 | г | 15 | в |
| 6 | б | 16 | б |
| 7 | б | 17 | б |
| 8 | в | 18 | б |
| 9 | в | 19 | в |
| 10 | б | 20 | б |


### Критерии оценивания итогового теста

| Количество правильных ответов | Баллы |
| :---: | :---: |
| 20 | 25 |
| 19 | 24 |
| 18 | 23 |
| 17 | 21 |
| 16 | 20 |
| 15 | 18 |
| 14 | 17 |
| 13 | 15 |
| 12 | 14 |
| 11 | 12 |
| 10 и менее | 0 |
