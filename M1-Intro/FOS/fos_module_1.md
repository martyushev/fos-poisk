### **Дисциплина «Модели и методы интеллектуального поиска»**

**Профессиональная роль:** ML Researcher

**ФОСы для модуля 1 «Введение в интеллектуальный поиск»**

**Лекция «Анатомия поиска: архитектура, ранжирование и искусственный интеллект»** — 2 час.

**Лабораторные работы** — 8 час.

---

### Блок 1. Текущий контроль (по лекционному курсу — 2 часа)

#### 1.1 Контрольные вопросы к лекции

1.  Опишите классическую конвейерную архитектуру поисковой системы. Какую функцию выполняет инвертированный индекс и почему он строится до этапа ранжирования?
2.  В чем заключается эволюция подходов к поиску: от булевых моделей и статистического TF-IDF/BM25 к семантическому векторному поиску? Назовите три ключевые технологические предпосылки этой смены парадигмы.
3.  Раскройте функции краулера и индексатора. Какие основные политики (politeness policy, deduplication) должен соблюдать современный промышленный краулер?
4.  Объясните принцип построения инвертированного списка. Что такое skip-lists и зачем они применяются в индексах промышленного масштаба?
5.  Перечислите классические факторы ссылочного ранжирования (PageRank, HITS). Почему сегодня статический PageRank уступил место динамическим факторам машинного обучения (*learning-to-rank*)?
6.  Что такое TrustRank и как он концептуально связан с борьбой со спамом? Как современные антиспам-фильтры используют графовые признаки?
7.  Каким образом модели трансформеров интегрируются в компоненты поисковой системы (приведите примеры для стадии индексации/матчинга и стадии финального ранжирования)?
8.  Опишите разницу между pointwise, pairwise и listwise подходами в Learning to Rank. Какой из них наиболее полно отражает задачу максимизации пользовательского удовлетворения?
9.  Дайте определение информационному пузырю (*filter bubble*) и эффекту Матфея в контексте современных рекомендательных и поисковых систем.
10. Чем отличается проверка качества поиска на старых данных офлайн от проверки в реальной работе системы онлайн? Приведите два примера, как понять, что пользователю нравится выдача, без прямых оценок. Почему хорошие результаты на тестовой выборке не всегда означают, что система понравится людям?
11. Как проблема «длинного хвоста» (long tail queries) влияет на стратегию сбора обучающих данных для ранжирующей модели?

#### 1.2 Практическое задание к лекции

**Тема:** Оценка качества ранжирования на синтетических данных с дисбалансом релевантности.

Студенту предоставляется набор данных `search_eval.csv`, содержащий результаты работы двух алгоритмов ранжирования (`model_baseline` — классический BM25, `model_ai` — нейросетевой рангер) для 1000 поисковых запросов.

**Структура файла:**
- `query_id`: идентификатор запроса.
- `doc_id`: идентификатор документа.
- `score_bm25`: оценка релевантности от классической модели.
- `score_nn`: оценка релевантности от AI-модели.
- `label`: истинная релевантность (2 — высокорелевантный, 1 — частично релевантный, 0 — нерелевантный).

Известно, что распределение label крайне неравномерное: ~85% документов имеют метку 0, ~10% — метку 1, ~5% — метку 2.

**Задачи:**

1.  **Анализ распределения.** Построить гистограмму распределения меток label. Рассчитать коэффициент Джини или энтропию для подтверждения степени дисбаланса.
2.  **Базовая оценка.** Отсортировать документы для каждого запроса по убыванию `score_bm25` и `score_nn`. Рассчитать для обеих моделей метрики: Mean Average Precision (MAP@10), Normalized Discounted Cumulative Gain (nDCG@10), Precision@1 и Recall@100.
3.  **Выявление проблемы.** Проанализировать полученные значения. Продемонстрировать численно, как высокий Recall@100 при низком Precision@1 у базовой модели маскирует её неспособность выводить релевантные документы в топ выдачи.
4.  **Борьба с дисбалансом (Sampling & Weighting):**
    - Реализовать перевыборку (*upsampling*) класса `label=2` внутри каждого батча с коэффициентом `k=3`.
    - Внедрить взвешенную версию nDCG, где gain для документов с `label=2` умножается на вес `α`, а с `label=1` — на `β` (α > β > 1). Например, для α из диапазона {3, 5, 7} и β из диапазона {1.5, 2.0, 2.5}, исключая комбинации, где α ≤ β.
5.  **Сравнение и выбор.** Пересчитать метрику nDCG@10 для обоих моделей с учетом сбалансированной выборки и новых весов. Сравнить дельту прироста между моделями. Обосновать, какая модель демонстрирует лучшую обобщающую способность после коррекции дисбаланса.
6.  **Отчетность.** Подготовить краткий аналитический отчет (до 2 страниц), включающий графики распределений, таблицы исходных и скорректированных метрик, обоснование выбранных гиперпараметров (α, β), анализ вычислительной сложности предложенных методов балансировки и итоговый вывод о применимости моделей в production-среде с учетом trade-off между качеством и latency.

#### 1.3 Вопросы для проверки практического задания к лекции

1.  Почему в условиях данного дисбаланса (~85% нулевых меток) стандартная метрика Accuracy (точность классификации) является бесполезной для сравнения моделей? Приведите пример «обманчивого» результата.
2.  Какую метрику из семейства ERR@k или nDCG@k следует выбрать для данной задачи и почему она лучше классического MAP при наличии трех уровней релевантности (0, 1, 2)? Обоснуйте различие через штраф за позицию.
3.  При расчете Precision@10 выяснилось, что `model_ai` находит 40 высокорелевантных документов (label=2) в топ-10 выдачи, а `model_baseline` — только 25. Однако по метрике Recall@100 нейросетевая модель проигрывает классической. Объясните этот парадокс с точки зрения распределения оценок `score_bm25` и `score_nn`.
4.  Предложите методологию честного сравнения алгоритмов, которая нейтрализует влияние дисбаланса классов без искусственного искажения структуры файла `search_eval.csv`. Какие две дополнительные бинарные метрики можно использовать для более полной оценки?
5.  Как наличие большого пласта частично релевантных документов (метка 1, ~10%) влияет на интерпретацию разрыва (gain) между `score_bm25` и `score_nn`? Опишите риск переобучения AI-модели на эту промежуточную группу.

---

### Блок 2. Лабораторный практикум (8 часов)

#### 2.1 Лабораторная работа

**Тема лабораторной работы:** Сравнительный анализ классических моделей информационного поиска на корпусе данных *Cranfield*.

**Цель лабораторной работы:** «Овладеть методологией оценки и сравнения эффективности классических моделей информационного поиска на стандартизированном тестовом наборе данных».

**Задание.** Предоставляется фрагмент классического корпуса *Cranfield* (48 документов — аннотаций статей по авиации, 225 запросов, бинарная матрица релевантности). Студенту необходимо:

1.  Реализовать с нуля или использовать библиотеки `scikit-learn` / `gensim` две различные классические модели представления документов и запросов:
    - Модель TF-IDF (векторное пространство).
    - Вероятностную модель BM25 (реализация через формулу Робертсона-Спарка-Джонса).
2.  Написать скрипт, который принимает текстовый запрос, вычисляет оценку релевантности (*score*) для каждого из 48 документов согласно обеим моделям и возвращает топ-10 результатов для каждой.
3.  Провести сравнительный анализ выдачи обеих моделей для трех тестовых запросов разной специфичности (например: «эффект земли», «турбулентность пограничного слоя», «композитные материалы»).

**Требования к лабораторной работе:**

1.  Git-репозиторий с тегом версии `submission`.
2.  Воспроизводимый ноутбук (Jupyter Notebook) или Python-скрипт, принимающий аргументы командной строки.
3.  PDF-отчет (2–3 страницы), содержащий таблицы с метриками (MAP@10, P@10, R@10), графики Precision-Recall и текстовый блок сравнительного анализа согласно критерию 3 продвинутого уровня.

**Формат сдачи:** Jupyter Notebook (`.ipynb`) или Python-скрипт (`.py`) с комментариями, содержащий код реализации, примеры ранжирования и аналитический отчет.

#### 2.2 Вопросы к защите лабораторной работы

1.  В чем заключается фундаментальное различие между векторной моделью (TF-IDF) и вероятностными моделями ранжирования (BM25)? Какую гипотезу о релевантности использует каждая из них?
2.  Запишите процесс определения веса термина в классической модели TF-IDF. Объясните физический смысл компонентов IDF (обратной частоты документа) при наличии шума в корпусе.
3.  Запишите основную формулу оценки релевантности BM25. Дайте определение параметрам `k1` и `b`. Какое влияние на итоговую выдачу окажет увеличение параметра `b` до значения, близкого к 1, в корпусе Cranfield, где документы имеют примерно одинаковую длину?
4.  Почему классическая бинарная модель (Boolean retrieval) принципиально не подходит для выполнения задания №3 лабораторной работы (сравнение топ-10 по специфичным запросам)?
5.  При расчете матрицы TF-IDF на фрагменте корпуса Cranfield студент обнаружил, что редкие технические термины получают аномально высокие веса IDF из-за малого размера коллекции (всего 48 документов). Назовите два стандартных метода сглаживания или нормализации IDF, которые предотвратят доминирование редких терминов над общеупотребительными авиационными понятиями.
6.  Чем отличается модель BM25F от модели BM25? Какого ключевого компонента (связанного со структурой документа) ей не хватает для полноценной обработки веб-документов, и почему на структурированном малом корпусе аннотаций Cranfield это несущественно?
7.  Как именно нормализация длины вектора (L2-нормализация) влияет на вычисление скалярного произведения в TF-IDF и почему она критически важна для корректного сравнения оценок *score* разных документов разной длины?

---

### Блок 3. Итоговый контроль

#### 3.1 Контрольные вопросы

1.  Объясните проблему размерности словаря в классической модели булева поиска. Как инвертированный индекс решает эту проблему на этапе индексации и как это влияет на асимптотику выполнения конъюнктивных и дизъюнктивных запросов?
2.  В чем заключается фундаментальное отличие эвристической оптимизации длины документа в формуле BM25 от нормализации косинуса угла в векторной модели TF-IDF? Приведите сценарии, где нормализация BM25 даст ложноположительный результат.
3.  Опишите этапы обработки запроса (*query processing*) в классическом конвейере поисковой системы. На каком этапе применяется расширение запроса (*query expansion*) с использованием псевдо-релевантной обратной связи (PRF) и какие риски это несет для точности (Precision)?
4.  Почему классическая вероятностная модель *Binary Independence Model (BIM)* считается теоретическим фундаментом BM25? Запишите ключевое допущение BIM о независимости терминов и объясните, как формула BM25 смягчает его негативные последствия на практике.
5.  Дайте определение понятия «разреженность вектора» в контексте VSM (*Vector Space Model*). Какие вычислительные преимущества дает использование разреженных матриц перед плотными эмбеддингами при поиске по коллекции из миллионов документов?
6.  Как закон Ципфа влияет на проектирование структуры обратного индекса и выбор порога отсечения редких терминов (*stopping criteria*)?
7.  В каких случаях классический подход Bag-of-Words (BoW) принципиально проигрывает методам, учитывающим позицию терма (например, *proximity search*), и как это связано с функциональной характеристикой полноты (Recall)?
8.  Объясните разницу между IDF (*Inverse Document Frequency*) и ICF (*Inverse Category Frequency*) в моделях латентной семантики. Какой из показателей более устойчив к шуму в веб-коллекциях?

#### 3.2 Тесты закрытого типа

1.  При расчете веса \(w_{ij}\) термина \(t_i\) в документе \(d_j\) по схеме TF-IDF, компонента IDF (обратная документная частота) служит для:
    - А) Увеличения веса часто встречающихся во всей коллекции слов (стоп-слов).
    - Б) Нормировки длины документа до единичного вектора.
    - В) Снижения веса общеупотребительных терминов и повышения веса специфичных для узкого подмножества документов.
    - Г) Учета порядка следования слов в предложении.

2.  В классической вероятностной модели BM25 параметр \(k_1\) отвечает за:
    - А) Степень наказания длинных документов.
    - Б) Вес применения оператору обязательного присутствия (+).
    - В) Насыщаемость частоты термина (верхний предел влияния роста tf на итоговую оценку релевантности).
    - Г) Коэффициент сглаживания при нулевых вероятностях.

3.  Что является основным недостатком чисто булевой модели поиска в промышленных системах?
    - А) Невозможность использования инвертированного индекса.
    - Б) Сложность реализации операторов AND, OR, NOT.
    - В) Бинарный характер вывода (документ либо полностью подходит, либо нет) и невозможность ранжирования результатов по степени соответствия.
    - Г) Высокая чувствительность к опечаткам в запросе.

4.  Для чего используется процедура стемминга (stemming) на этапе токенизации?
    - А) Для приведения словоизменительных форм слова к единой базовой форме (стемме) с целью увеличения совпадений с запросом.
    - Б) Для удаления знаков препинания и спецсимволов.
    - В) Для перевода текста на английский язык.
    - Г) Для выделения именованных сущностей (NER).

5.  Матрица «термин-документ» в классической векторной модели пространства (VSM) обычно обладает свойством:
    - А) Плотности, так как большинство терминов встречается в большинстве документов.
    - Б) Высокой разреженности (более 95-99% элементов равны нулю).
    - В) Строгой симметричности относительно главной диагонали.
    - Г) Квадратной формы, где число терминов всегда равно числу документов.

6.  Псевдо-релевантная обратная связь (Pseudo-Relevance Feedback, PRF) предполагает:
    - А) Обязательное взаимодействие с пользователем после первого шага поиска.
    - Б) Автоматическое расширение исходного запроса терминами из топ-N предположительно релевантных документов первой выдачи без подтверждения пользователя.
    - В) Поиск только по заголовкам документов.
    - Г) Удаление всех стоп-слов из расширенного запроса.

7.  Косинусная мера сходства (Cosine Similarity) в VSM предпочтительнее скалярного произведения векторов, потому что она:
    - А) Вычисляется быстрее процессором.
    - Б) Игнорирует евклидову длину (норму) документа, фокусируясь на угле между вектором запроса и документом, что нивелирует влияние объема текста.
    - В) Работает только с бинарными векторами.
    - Г) Требует предварительного обучения нейросети.

8.  Параметр \(b\) в формуле BM25 (\(0 \le b \le 1\)) контролирует:
    - А) Общую длину поискового запроса.
    - Б) Минимальную частоту появления термина в документе.
    - В) Степень влияния нормализации длины документа (насколько сильно длинные документы штрафуются относительно коротких).
    - Г) Количество документов, возвращаемых пользователю (page size).

9.  Инвертированный индекс (Inverted Index) в поисковых системах представляет собой структуру данных, которая сопоставляет:
    - А) Документ со списком других похожих документов.
    - Б) Хеш-сумму документа с его путем на диске.
    - В) Термин (лексему) со списком идентификаторов документов (Postings List), в которых он встречается.
    - Г) Позицию курсора пользователя с координатами на экране.

10. Основная цель применения LSI/LSA (латентно-семантического индексирования/анализа) в информационном поиске:
    - А) Максимально точное сохранение пунктуации оригинального текста.
    - Б) Преодоление проблемы синонимии (разные слова об одном понятии) и полисемии (одно слово в разных значениях) через проекцию в пространство меньшей размерности.
    - В) Увеличение физического размера индекса на жестком диске.
    - Г) Замена TF-IDF на простую частоту терминов.

11. На каком этапе классического поискового пайплайна происходит преобразование сырого текста документов в структуру инвертированного индекса?
    - А) Краулинг
    - Б) Индексация
    - В) Ранжирование
    - Г) Выдача результатов (Serving)

12. Основная цель внедрения эмбеддингов (векторных представлений) текстов в поисковую систему — это:
    - А) Увеличение скорости обхода интернета краулером
    - Б) Уменьшение физического размера индекса на диске
    - В) Переход от лексического соответствия (совпадения слов) к семантическому сходству смыслов
    - Г) Автоматическая очистка HTML-тегов от мусора

13. Метрика NDCG (Normalized Discounted Cumulative Gain) отличается от DCG тем, что она:
    - А) Не учитывает позицию релевантного документа
    - Б) Работает только с бинарными метками релевантности (0 или 1)
    - В) Нормализует значение идеальными (best possible) результатами для конкретного набора релевантностей, позволяя сравнивать разные запросы
    - Г) Использует логарифмическое основание 10 вместо натурального логарифма

14. При расчете PageRank страница передает свой «вес» другим страницам. Что произойдет с суммой весов во всей сети, если в ней присутствуют страницы без исходящих ссылок (dangling nodes)?
    - А) Сумма весов будет строго равна количеству страниц
    - Б) Ничего не изменится
    - В) Сумма весов начнет уменьшаться («утекать» из системы) без специальных корректировок матрицы переходов
    - Г) Алгоритм зациклится и не сойдется

15. Какое фундаментальное ограничение есть у классической модели BM25 по сравнению с современными нейросетевыми ранкерами?
    - А) Высокая вычислительная сложность на этапе индексации
    - Б) Неспособность работать с текстами длиннее 512 токенов
    - В) Неспособность учитывать контекст слова и синонимию (лексический разрыв / vocabulary mismatch)
    - Г) Зависимость от наличия внешних ссылочных графов

16. Проблема холодного старта (cold start) в поиске чаще всего возникает, когда:
    - А) Сервер выдач перезагружается после обновления кода
    - Б) Появляется новый документ или новый запрос, для которых еще нет накопленных поведенческих сигналов или качественных признаков
    - В) Заканчивается бюджет на серверные мощности
    - Г) Происходит массовая DDOS-атака на краулер

17. В задаче LTR (Learning to Rank) подход ListWise (например, LambdaLoss) оптимизируется функция потерь, которая:
    - А) Минимизирует ошибку предсказания оценки для одного отдельного документа
    - Б) Штрафует модель за неправильный порядок каждой пары соседних документов
    - В) Оптимизирует метрику качества (NDCG/MAP) напрямую на всем списке кандидатов (плейсменте)
    - Г) Максимизирует количество кликов независимо от их порядка

18. Что является главным признаком несбалансированности данных при оценке поисковой модели?
    - А) Низкое время ответа сервера (latency)
    - Б) Резкое преобладание класса нерелевантных документов (`label=0`) над релевантными, ведущее к завышению точности при случайном угадывании
    - В) Наличие дублей документов в выдаче
    - Г) Использование GPU вместо CPU для инференса

19. Для борьбы с дисбалансом классов в обучении ранжирующей модели исследователь решил применить downsampling мажоритарного класса. Каков основной риск этого подхода в промышленной системе?
    - А) Модель станет переобученной
    - Б) Резко возрастет потребление оперативной памяти
    - В) Потеря полезной информации из отброшенных негативных примеров, что может привести к снижению разнообразия (diversity) выдачи
    - Г) Невозможность применения градиентного бустинга

20. Офлайн-метрика показывает отличный рост MRR, но A/B-тест на реальных пользователях показывает падение конверсии. Наиболее вероятная причина:
    - А) Ошибка в коде фронтенда
    - Б) Оптимизация под прокси-метрики (ранжирование известных кликов) в ущерб обнаружению нового полезного контента (проблема position bias / selection bias)
    - В) Недостаточный размер тестовой выборки в офлайне
    - Г) Неправильный расчет IDF в инвертированном индексе

---

#### 3.3 Деловая игра «Архитектор баланса: от несбалансированных данных к надежному ИИ»

**Паспорт деловой игры**

**Дисциплина:** Модели и методы интеллектуального поиска (в контексте ML — поиск оптимальных архитектур, гиперпараметров и стратегий обучения).

**Профессиональная роль:** ML Researcher (Исследователь в области машинного обучения). Акцент на исследовательский цикл: гипотеза → эксперимент → анализ артефактов → новая гипотеза.

**Формируемые компетенции ФГОС 3++ (примерные):**
- ML-2.3: Решает проблемы несбалансированных данных и оценивает качество моделей.
- ML-3.2: Эффективно применяет классические методы и модели МО для обеспечения достижимости функциональных характеристик систем ИИ.

**Игровой сеттинг и легенда**

Игроки выступают в роли ведущих исследователей R&D-отдела компании, разрабатывающей систему предиктивного технического обслуживания (*Predictive Maintenance*) для критической инфраструктуры (например, диагностика отказов авиационных двигателей или турбин ГЭС).

Заказчик предоставил исторические данные телеметрии за 5 лет. Система должна с высокой точностью предсказывать скорый отказ узла (класс Failure) за 48 часов до события, чтобы успеть провести ремонт. **Проблема:** Класс Failure составляет всего 0.8% выборки (крайне сильный дисбаланс). Стандартные метрики точности показывают 99%, но система бесполезна — она просто всегда предсказывает класс Normal. До релиза системы осталось две недели. Команда должна найти решение, которое пройдет приемочные тесты заказчика.

Игра рассчитана на команду из 3–5 человек (или индивидуальную работу) и длится 4 академических часа (с перерывом).

| Этап | Время | Действия команды (процесс исследования) | Роль преподавателя (фасилитатора) |
| :--- | :--- | :--- | :--- |
| **Этап 0. Брифинг и вводные** | 20 мин | Получение датасета (`train.csv`, скрытый `test.csv`), бизнес-требований (FPR <5%, Recall > 95%), ограничений (инференс <50 мс). Первичный EDA. | Выдает материалы, отвечает на вопросы по доменному контексту, не дает советов по ML. |
| **Этап 1. Базлайн и осознание проблемы** | 40 мин | Обучение простой модели (Logistic Regression / Random Forest без балансировки). Расчет метрик: Accuracy (~99.2%), Precision/Recall/F1 (~близко к нулю), Confusion Matrix. Формулировка первой исследовательской гипотезы о причине провала. | Наблюдает. Вмешивается только если команда использует неверную формулу метрики. |
| **Этап 2. Хакатон методов ресемплинга (ML-2.3)** | 60 мин | Командный мозговой штурм. Параллельная реализация минимум трех подходов к работе с дисбалансом: <br><br> Over-sampling (SMOTE, ADASYN) <br> Under-sampling (RandomUnderSampler, Tomek Links) <br> Комбинированные методы (SMOTETomek) <br> Синтетические подходы (Data Augmentation в фичевом пространстве) <br><br> Оценка влияния каждого метода на метрику F-beta (β=2, так как важнее полнота) и дисперсию модели. | Ходит между командами, задавая провоцирующие вопросы: «Почему SMOTE порождает новые ошибки в вашем случае?», «Как шум в мажоритарном классе влияет на under-sampling?». |
| **Coffee Break** | 15 мин | Неформальное обсуждение промежуточных результатов. | |
| **Этап 3. Архитектурный стек и алгоритмические решения (ML-3.2)** | 60 мин | Отказ от простых ресемплеров в пользу изменения функции потерь и архитектуры поиска: <br><br> Использование `class_weight` / `scale_pos_weight`. <br> Переход к алгоритмам, устойчивым к дисбалансу (Gradient Boosting — CatBoost/LGBM, сбалансированные леса BalancedBaggingClassifier). <br> Применение пороговых трансформаций (Threshold Moving) на основе калибровки вероятностей (Platt Scaling, Isotonic Regression). <br><br> Ранжирование решений по Pareto-фронту (Trade-off между Recall и временем инференса). | Следит за тем, чтобы студенты аргументировали выбор именно классических методов через их математические свойства применительно к задаче. |
| **Этап 4. Корректная оценка и защита (ML-2.3 + ML-3.2)** | 45 мин | Выбор финального пайплайна. Проведение слепого тестирования на отложенной выборке. Построение Precision-Recall кривой, расчет AUPRC, PR-AUC. Анализ стоимости ошибок (Business-oriented metrics: Cost Matrix). Подготовка отчета исследователя (краткий Markdown/Jupyter Notebook с визуализациями trade-offs). | Проводит экспресс-код-ревью выбранных реализаций. Проверяет воспроизводимость (наличие seed, версий библиотек). |
| **Этап 5. Защита перед заказчиком** | 20 мин | Презентация (3 минуты + 2 минуты Q&A). Команда доказывает, почему их решение обеспечивает требуемые функциональные характеристики (Recall > 95%) и экономически оправдано. | Выступает в роли сурового СТО заказчика. Отклоняет аргументы вида «наш Accuracy самый высокий». Принимает только обоснования на базе Recall, AUPRC и стоимости ложноотрицательных срабатываний. |

**Инструментарий и ограничения**

- **Язык:** Python.
- **Библиотеки:** `pandas`, `numpy`, `scikit-learn` (обязательно использование `Pipeline` для предотвращения data leakage при ресемплинге), `imbalanced-learn`, `catboost`/`lightgbm`, `matplotlib`/`seaborn`.
- **Ограничения:** Запрещено использовать ансамбли сложных нейросетей (TabNet, DeepFM) — задача решается классическими методами. Запрещено менять тестовую выборку.

**Ключевые вопросы:**
- Какая ошибка стоила вашей команде больше всего времени?
- Почему ROC-AUC оказался обманчиво высоким (часто > 0.98), тогда как бизнес-метрики были катастрофическими?
- Какой метод вы выбрали бы в продакшене завтра утром и почему именно его? Что станет вашим следующим шагом как исследователей, если проект продлится еще на месяц?
